<?php
/**
 * Application.php
 *
 * Glyph library
 */


/**
 * Takes a filename and returns a string containing the file extension.
 * @input filename (Ex. myglyphfile.glyph)
 * @output extension (Ex. glyph)
 */
function getFileExtension($ext=null) {
    if ( $ext ) {
        // do something
    } else {
        return false;
    }
}
